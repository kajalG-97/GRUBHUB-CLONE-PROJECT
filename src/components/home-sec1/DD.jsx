import { NAME } from "./style"

export const DarkDiv = () => {
    return (
        <NAME>
            <h4>subscription passUnlimited $0 delivery fees and a $10 free meal each month!</h4>
        </NAME>
    )
}